# Scriptism – Simple

This simple command line app asks name, and prents out a welcome message. But if name is "Tom" it asks if he is evil or not, then prints a message accordinly.

## How to run

```bash
python main.py
```
